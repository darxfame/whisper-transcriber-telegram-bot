# Все текстовые файлы — LF
* text=auto eol=lf

# Shell-скрипты — СТРОГО LF, иначе сломаются на Linux
*.sh text eol=lf

# Python — LF
*.py text eol=lf

# Конфиги — LF
*.yml text eol=lf
*.yaml text eol=lf
Dockerfile text eol=lf
.env text eol=lf

# Бинарники — без конвертации
*.session binary
*.png binary
*.jpg binary
