# ⚡ Quick Start Guide

## 🚀 Быстрый запуск за 5 минут

### Шаг 1: Получить API_ID и API_HASH
```
1. Перейти на https://my.telegram.org/apps
2. Получить App ID и App Hash
```

### Шаг 2: Подготовить .env
```bash
# Копировать пример
cp .env.example .env

# Отредактировать (используйте свой редактор)
# Вставить ваши API_ID и API_HASH
cat .env
```

Итоговый .env должен выглядеть так:
```bash
API_ID=26607062
API_HASH=your_api_hash_here
BOT_TOKEN=
FRIEND_USER_ID=0
WHISPER_MODEL=small
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
```

### Шаг 3: Запустить Docker Compose
```bash
# Единая команда для запуска всего
docker-compose up -d

# Проверить статус (должно быть "healthy" для Redis)
docker-compose ps

# Следить за логами
docker-compose logs -f userbot
```

### Шаг 4: Авторизация (первый запуск)
При первом запуске приложение запросит авторизацию в Telegram в логах:

```
📋 Требуется авторизация
Введите номер телефона (международный формат, например +1234567890):
```

Введите телефон номер своего аккаунта и подтвердите код.

### Шаг 5: Готово! 🎉
```bash
# Проверить, что работает
docker-compose exec userbot python -c "import redis; r=redis.Redis(host='redis'); print('✅ Connected:', r.ping())"

# Или просто отправить боту команду
# /status
```

---

## 📱 Использование

### Включить/выключить:
```
/voicebot_on    — включить всё
/voicebot_off   — выключить всё
```

### Обработка голосовых:
Просто отправьте голосовое сообщение и бот автоматически его транскрибирует!

### Добавить друга для отслеживания:
```
1. Ответить на его сообщение
2. Отправить /addtovoicebot
```

### Переключить модель:
```
/model tiny      — быстро (худшее качество)
/model small     — баланс ⭐ рекомендуется
/model large     — качество (медленно)
```

---

## 🔧 Команды для управления

```bash
# Просмотреть логи
docker-compose logs -f userbot

# Перезапустить бот
docker-compose restart userbot

# Остановить
docker-compose down

# Проверить здоровье
bash health_check.sh

# Войти в контейнер
docker-compose exec userbot bash
```

---

## ⚠️ Если что-то пошло не так

### Ошибка "API_ID not found"
```bash
# Проверить .env
cat .env

# Должны быть заполнены:
API_ID=ВАШ_ID
API_HASH=ВАШ_HASH
```

### Ошибка "Redis connection refused"
```bash
# Перезапустить Redis
docker-compose restart redis

# Если не помогает
docker-compose down
docker-compose up -d --build
```

### Бот не обрабатывает голосовые
```bash
# Проверить, включен ли
/status

# Если выключен
/voicebot_on
```

### Полная переустановка
```bash
docker-compose down -v
rm voice_transcriber.session
docker-compose up -d --build
```

---

## 📊 Требования

**Минимум:**
- Docker & Docker Compose
- 4GB RAM
- 2 CPU cores

**Рекомендуется (на Proxmox LXC):**
- 16GB RAM
- 4-8 CPU cores
- 20GB свободного места на диске

---

## 📖 Дополнительная информация

- **README.md** — полная документация
- **TROUBLESHOOTING.md** — решение 12 типичных проблем
- **CHANGELOG.md** — список всех исправлений

---

## ✅ Чек-лист первого запуска

- [ ] Docker установлен (`docker --version`)
- [ ] Docker Compose установлен (`docker-compose --version`)
- [ ] .env файл заполнен API_ID и API_HASH
- [ ] `docker-compose up -d` выполнен без ошибок
- [ ] `docker-compose ps` показывает оба контейнера
- [ ] Логи показывают "Соединение с Telegram установлено"
- [ ] Бот отвечает на команду `/status`
- [ ] Голосовые обрабатываются

---

**Готово! Бот работает и автоматически переподключается при разрыве сети.**

Если нужна помощь, смотрите **TROUBLESHOOTING.md**

