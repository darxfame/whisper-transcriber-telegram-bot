# 🚀 Быстрая настройка кэша на Proxmox LXC

## За 5 минут включить BuildKit для молниеносных пересборок

### 1️⃣ Включить BuildKit в Docker

```bash
# SSH в Proxmox хост или контейнер с Docker

# Отредактировать конфиг
nano /etc/docker/daemon.json

# Вставить:
{
  "features": {
    "buildkit": true
  }
}

# Сохранить (Ctrl+O, Enter, Ctrl+X)

# Перезагрузить Docker
systemctl restart docker

# Проверить
docker buildx ls
```

### 2️⃣ Тестировать скорость

```bash
cd /path/to/pyrofork

# Первая сборка (медленно, но нормально)
docker-compose up -d --build

# Вторая сборка (должна быть в 100 раз быстрее!)
docker-compose down
docker-compose up -d --build    # должно быть 3-5 секунд!
```

### 3️⃣ Готово! ✅

Теперь все NVIDIA зависимости:
- Первый раз: скачиваются и кэшируются (5-10 мин)
- Потом: берутся из кэша Docker (3-5 сек)

---

## Что произойдет

```
Первая сборка:
Installing collected packages: nvidia-cuda-runtime-cu12, 
nvidia-cublas-cu12, nvidia-cudnn-cu12, torch, transformers, 
faster-whisper, ...
(все скачивается и компилируется)

Вторая пересборка:
[использует BuildKit кэш - быстро!]
✅ Готово за 3-5 секунд
```

---

**Вот и всё!** Больше не будет долгого скачивания NVIDIA пакетов при каждом редеплое.

