# 📋 CODE REVIEW GUIDELINES

Правила проверки кода перед коммитом и деплоем.

## 1️⃣ Орфографические и синтаксические ошибки

### Проверяем:
- ✅ Все переменные названы правильно (snake_case для Python)
- ✅ Функции и методы названы понятно
- ✅ Комментарии написаны на русском БЕЗ опечаток
- ✅ Нет дублирующихся слов ("the the", "что что")
- ✅ Все строки завершаются правильно
- ✅ Кавычки парные (не смешаны " и ')
- ✅ Скобки сбалансированы ()[]{}
- ✅ Отступы консистентны (4 пробела для Python)

### Инструменты:
```bash
# Проверка синтаксиса Python
python -m py_compile userbot.py

# Проверка линтером (если доступен)
flake8 userbot.py  # если установлен
pylint userbot.py  # если установлен

# Проверка на орфографию в комментариях
grep -r "TODO\|FIXME\|XXX" userbot.py  # найти незавершенные задачи
```

---

## 2️⃣ Логические ошибки

### Проверяем:
- ✅ Переменные инициализированы перед использованием
- ✅ Функции возвращают то, что обещают
- ✅ Исключения обработаны правильно (не молча падают)
- ✅ Асинхронные функции (async) вызываются с await
- ✅ Нет бесконечных циклов без выхода
- ✅ Ресурсы закрываются (файлы, соединения)
- ✅ Логический поток понятен (if/else имеют смысл)
- ✅ Нет противоречащих условий

### Пример проверки:
```python
# ❌ НЕПРАВИЛЬНО: переменная используется перед инициализацией
if x > 0:
    print(x)
# x не определена!

# ✅ ПРАВИЛЬНО:
x = 5
if x > 0:
    print(x)
```

---

## 3️⃣ Правило 5 WHY для выявления корневых причин

**Когда используем:** При нахождении ошибки или несоответствия.

**Процесс:**
1. Описываем проблему
2. Спрашиваем "Почему?" 5 раз
3. На каждом уровне ищем более глубокую причину
4. Находим корневую причину
5. Исправляем именно её (не симптом)

### Пример из проекта:

**Проблема:** "Приложение валится при разрыве сети"

```
1️⃣ Почему валится?
   → Нет обработки исключения ConnectionError

2️⃣ Почему нет обработки?
   → Функция main() не ловит это исключение

3️⃣ Почему не ловит?
   → Код написан без retry логики

4️⃣ Почему без retry логики?
   → Разработчик не предусмотрел разрывы соединения

5️⃣ Почему не предусмотрел?
   → Требование было "запустить и работать", а не "быть отказоустойчивым"

КОРНЕВАЯ ПРИЧИНА: Неправильное требование к надежности системы
РЕШЕНИЕ: Добавить переподключение с exponential backoff
```

---

## 4️⃣ Ссылки на источники

### Правило:
**НИКОГДА** не пишу и не предлагаю то, чего не знаю или не могу обосновать.

### Если использую чужой код/идею:
- ✅ Оставляю ссылку на источник в комментарии
- ✅ Указываю лицензию (если требуется)
- ✅ Ссылаюсь на документацию стандартов

### Пример с ссылками:

```python
# Exponential backoff алгоритм
# https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
retry_delay = min(retry_delay * 2, max_delay)

# Graceful shutdown обработка сигналов
# https://docs.python.org/3/library/signal.html
signal.signal(signal.SIGTERM, signal_handler)

# Многоэтапная Docker сборка
# https://docs.docker.com/build/building/multi-stage/
FROM python:3.12-slim as builder
```

### Когда НЕ знаю точно:
- Говорю "Не уверен, нужна проверка"
- Предлагаю несколько вариантов
- Указываю источники для проверки
- Не угадываю "в лоб"

---

## 5️⃣ Чек-лист перед каждым коммитом

### Перед тем как сказать "готово":

- [ ] Синтаксис проверен (`python -m py_compile`)
- [ ] Нет орфографических ошибок в коде и комментариях
- [ ] Логика понятна (даже новичку)
- [ ] Все исключения обработаны
- [ ] Использованы только известные технологии/библиотеки
- [ ] Есть ссылки на источники для нестандартных решений
- [ ] Правило 5 WHY применено для сложных проблем
- [ ] Документация обновлена (если нужно)
- [ ] Тесты проходят (если есть)
- [ ] Код работает локально/в контейнере

### Пример из этого проекта:

```
✅ Синтаксис: python -m py_compile userbot.py
✅ Орфография: все функции названы правильно, комментарии без опечаток
✅ Логика: функция load_config() проверяет API_ID и API_HASH перед использованием
✅ Исключения: try/except обрабатывает ConnectionError, OSError, TimeoutError
✅ Источники: многоэтапная сборка ссылается на Docker docs
✅ 5 WHY: проблема с ConnectionError -> корень в неправильном импорте pyrogram
✅ Документация: DOCKER_OPTIMIZATION.md создана и ссылается на Docker docs
✅ Работает: Dockerfile синтаксически верный, userbot.py компилируется
```

---

## 6️⃣ Примеры решений с источниками

### Хороший пример:

```python
# Проблема: При разрыве сети приложение падает
# Источник: https://aws.amazon.com/blogs/architecture/exponential-backoff-and-jitter/
# Решение: exponential backoff с max delay 300s

retry_delay = RECONNECT_DELAY  # 5s
max_delay = MAX_RECONNECT_DELAY  # 300s
RECONNECT_BACKOFF = 2

while True:
    try:
        await app.start()
        retry_delay = RECONNECT_DELAY  # сброс после успеха
    except (ConnectionError, OSError, TimeoutError) as e:
        logger.error(f"Connection error: {e}")
        await asyncio.sleep(retry_delay)
        retry_delay = min(retry_delay * RECONNECT_BACKOFF, max_delay)
```

### Плохой пример (НЕ ДЕЛАЮ):

```python
# Попытка решить проблему, которую не понимаю
# "А может быть, если я добавлю магические числа?"
retry_delay = 0.5
while ...:
    retry_delay += 0.5  # просто увеличиваем на 0.5 секунды
    # Непонятно почему именно 0.5, нет источника, нет обоснования
```

---

## 7️⃣ Политика "Не знаю" vs "Знаю"

### Когда говорю "знаю":
- Тестировал код лично
- Есть ссылка на официальную документацию
- Объясню логику в 2-3 предложениях
- Готов ответить на вопросы "почему"

### Когда говорю "не уверен":
- Предполагаю решение, но не 100% уверен
- Предлагаю несколько вариантов
- Даю ссылки для проверки
- Говорю прямо: "Нужна проверка"

### Пример из проекта:

```
✅ ЗНАЮ: Docker многоэтапная сборка экономит образ
   Источник: https://docs.docker.com/build/building/multi-stage/
   Проверил: сам писал Dockerfile

❓ НЕ УВЕРЕН: Будет ли BuildKit работать на всех версиях Docker
   Источник: https://docs.docker.com/build/architecture/
   Требует проверки на Proxmox с конкретной версией Docker
   Варианты: включить BuildKit или использовать обычный Docker builder
```

---

## 📋 Итоговое правило

**ПЕРЕД ТЕМ КАК ПРЕДЛОЖИТЬ КОД ИЛИ РЕШЕНИЕ:**

1. ✅ Проверяю орфографию (особенно в коде и комментариях)
2. ✅ Проверяю логику (следует ли из одного другое?)
3. ✅ Применяю правило 5 WHY (в чем КОРНЕВАЯ причина?)
4. ✅ Собираю источники (где это задокументировано?)
5. ✅ Честно говорю о степени уверенности (знаю/предполагаю/не знаю)
6. ✅ Запускаю финальный чек-лист
7. ✅ ТОЛЬКО ПОТОМ говорю "готово"

---

**Дата:** 2026-02-18  
**Версия:** 1.0  
**Статус:** ✅ Активные правила для всех изменений в проекте
