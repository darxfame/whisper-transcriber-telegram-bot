# 🔧 INSTALL - Полное руководство по установке

## 📋 Содержание исправленного проекта

```
pyrofork/
├── 🆕 userbot.py              ← Исправленный основной файл (30.9 KB)
├── 📦 userbot_old.py          ← Резервная копия старой версии (22.9 KB)
│
├── 🐳 docker-compose.yml      ← Обновленный Docker Compose (1.0 KB)
├── 🐳 Dockerfile              ← Обновленный Dockerfile
│
├── 📝 requirements.txt         ← Обновленные зависимости (171 B)
├── .env                        ← Ваша конфигурация (ЗАПОЛНИТЬ!)
├── .env.example                ← Пример конфигурации
├── .dockerignore               ← Исключение файлов из образа
│
├── 🚀 start.sh                 ← Скрипт запуска (1.6 KB)
├── 🏥 health_check.sh          ← Проверка здоровья (2.3 KB)
│
├── 📖 README.md                ← Полная документация (10.2 KB)
├── 🚨 TROUBLESHOOTING.md       ← Решение проблем (11.5 KB)
├── 📋 CHANGELOG.md             ← Список исправлений (12.4 KB)
├── ⚡ QUICKSTART.md            ← Быстрый старт (5.1 KB)
└── 🔧 INSTALL.md              ← Этот файл

voice_transcriber.session      ← Сессия Telegram (создается при первом запуске)
```

---

## ✅ Что было исправлено

### Основная проблема
**До:** Приложение валилось при разрыве сети и не восстанавливалось  
**После:** Автоматическое переподключение с экспоненциальной задержкой

### Все исправления
1. ✅ Переход с `pyrofork` на `pyrogram` (2.0.106+)
2. ✅ Правильное переподключение при разрыве сети
3. ✅ Обработка всех типов ошибок соединения
4. ✅ Graceful shutdown (SIGTERM/SIGINT)
5. ✅ Health check для Redis
6. ✅ Валидация конфигурации при старте
7. ✅ Обработка ошибок загрузки моделей
8. ✅ Логирование всех критических событий
9. ✅ Полная документация (40KB+)
10. ✅ Гайды по решению 12 типичных проблем

---

## 🚀 Установка за 5 шагов

### Шаг 1️⃣: Получить API данные

1. Перейти на https://my.telegram.org/apps
2. Авторизоваться с вашим номером телефона
3. Создать новое приложение или скопировать существующее
4. Получить:
   - **App ID** → это ваш `API_ID`
   - **App hash** → это ваш `API_HASH`

### Шаг 2️⃣: Заполнить конфигурацию

Все файлы уже готовы, нужно только настроить .env:

```bash
# Копировать пример (если .env не существует)
cp .env.example .env

# Открыть и отредактировать (используйте любой редактор)
# nano .env
# vim .env
# code .env
# или просто отредактируйте в IDE

# Должно выглядеть так:
API_ID=26607062
API_HASH=your_api_hash_here
BOT_TOKEN=
FRIEND_USER_ID=0
WHISPER_MODEL=small
```

### Шаг 3️⃣: Проверить Docker

```bash
# Проверить наличие Docker
docker --version
# Должно вывести версию, например: Docker version 24.0.0

# Проверить Docker Compose
docker-compose --version
# Должно вывести версию, например: Docker Compose version 2.20.0

# Если не установлены - скачать с https://docs.docker.com/get-docker/
```

### Шаг 4️⃣: Запустить проект

```bash
# Перейти в директорию проекта
cd /path/to/pyrofork

# Запустить всё в фоне
docker-compose up -d

# Проверить статус
docker-compose ps

# Вывод должен показать:
# NAME              STATUS
# voicebot-redis    Up (healthy) ✅
# voicebot-userbot  Up (starting) ⏳ → (healthy) ✅
```

### Шаг 5️⃣: Авторизация

При первом запуске бот запросит авторизацию:

```bash
# Следить за логами
docker-compose logs -f userbot

# Вы увидите:
# 📋 Требуется авторизация
# Введите номер телефона (международный формат, например +1234567890):

# Введите свой номер телефона (где вы авторизованы в Telegram)
# Затем подтвердите код из Telegram

# Если есть 2FA:
# Введите пароль: (пароль от 2FA)

# После этого вы увидите:
# ✅ Соединение с Telegram установлено
```

### 🎉 Готово!

Теперь бот работает и:
- ✅ Автоматически обрабатывает голосовые сообщения
- ✅ Переподключается при разрыве сети
- ✅ Сохраняет состояние в Redis
- ✅ Логирует всё важное

---

## 📱 Первый запуск

### Проверить, что работает

```bash
# Отправить боту команду /status
/status

# Получить ответ:
# 📊 **Статус:**
# Глобально: ✅
# Свои: ✅
# Чужие: ✅
# Модель: `small`
# CPU: 8 потоков
# Отслеживается: 0 польз.
```

### Попробовать обработку голоса

1. Отправить боту голосовое сообщение
2. Бот ответит с транскрипцией
3. Может занять 5-30 сек в зависимости от модели

### Добавить друга

```bash
# Ответить на сообщение друга
/addtovoicebot

# Теперь голосовые от друга будут автоматически транскрибироваться
```

---

## 🔧 Команды управления

### Запуск/остановка

```bash
# Запустить
docker-compose up -d

# Остановить (graceful)
docker-compose down

# Перезапустить
docker-compose restart userbot

# Пересоздать контейнеры (например, после изменения .env)
docker-compose down
docker-compose up -d --build
```

### Просмотр логов

```bash
# Последние логи
docker-compose logs userbot --tail=50

# В реальном времени
docker-compose logs -f userbot

# Только ошибки
docker-compose logs userbot | grep ERROR

# За последний час
docker-compose logs --since=1h userbot
```

### Проверка здоровья

```bash
# Автоматическая проверка всей системы
bash health_check.sh

# Вывод:
# 🏥 Health Check VoiceBot
# [1] Docker installed... ✅ OK
# [2] Docker running... ✅ OK
# ...
# Summary: 12/12 checks passed
# ✅ All checks passed!
```

### Диагностика проблем

```bash
# Проверить Redis
docker-compose exec redis redis-cli PING

# Проверить Python версию
docker-compose exec userbot python --version

# Проверить сеть из контейнера
docker-compose exec userbot ping 8.8.8.8

# Проверить установленные пакеты
docker-compose exec userbot pip list | grep -E "pyrogram|redis|faster"

# Интерактивная оболочка
docker-compose exec userbot bash
```

---

## 🐛 Типичные проблемы при установке

### Проблема: "docker-compose: command not found"

**Решение:**
```bash
# На новых версиях Docker используйте
docker compose up -d  # без дефиса

# Или установите docker-compose отдельно
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose
```

### Проблема: "Permission denied: './start.sh'"

**Решение:**
```bash
# Сделать скрипт исполняемым
chmod +x start.sh
chmod +x health_check.sh
```

### Проблема: ".env file not found"

**Решение:**
```bash
# Создать файл
cp .env.example .env

# Отредактировать
nano .env  # or vim, or code, etc.

# Убедиться, что заполнены
cat .env
```

### Проблема: "ValueError: API_ID is required"

**Решение:**
```bash
# Проверить .env
cat .env

# API_ID ДОЛЖНА быть заполнена (число)
API_ID=26607062  # ✅ Правильно

# Перезапустить
docker-compose down
docker-compose up -d
```

### Проблема: Redis не запускается

**Решение:**
```bash
# Проверить логи Redis
docker-compose logs redis

# Если есть ошибки - пересоздать
docker-compose down
docker volume rm pyrofork_redis_data  # удалить данные
docker-compose up -d redis

# Проверить
docker-compose exec redis redis-cli PING
```

---

## 📊 Требования системы

### Минимальные
- **ОС:** Linux, macOS, Windows 10+
- **Docker:** 20.10+
- **RAM:** 4GB (для tiny модели)
- **CPU:** 2 cores
- **Диск:** 10GB

### Рекомендуемые (для Proxmox LXC)
- **ОС:** Ubuntu 22.04 LTS
- **Docker:** 24.0+
- **RAM:** 16GB (для small-medium)
- **CPU:** 4-8 cores
- **Диск:** 50GB (для кэша моделей)

### Максимальные (из docker-compose.yml)
```yaml
limits:
  memory: 50G
  cpus: '8'
reservations:
  memory: 16G
  cpus: '4'
```

---

## 📚 Документация

После установки смотрите:

1. **QUICKSTART.md** - быстрый старт за 5 минут
2. **README.md** - полное описание функций
3. **TROUBLESHOOTING.md** - решение 12 типичных проблем
4. **CHANGELOG.md** - что было исправлено

---

## 🔒 Безопасность

### Чувствительные данные
- ❌ НЕ коммитьте .env файл в git
- ❌ НЕ делитесь API_HASH или BOT_TOKEN
- ✅ Используйте .env.example как шаблон
- ✅ Добавьте .env в .gitignore

```bash
# Пример .gitignore
.env
.env.local
voice_transcriber.session
redis_data/
__pycache__/
logs/
```

### Права доступа
```bash
# Установить правильные права
chmod 600 .env                        # только владелец может читать
chmod 755 start.sh health_check.sh    # исполняемые скрипты
```

---

## 🆘 Если что-то пошло не так

### Проверить шаги по порядку

1. ✅ Docker установлен и работает
2. ✅ .env файл существует и заполнен
3. ✅ `docker-compose up -d` выполнена без ошибок
4. ✅ `docker-compose ps` показывает оба контейнера
5. ✅ `docker-compose logs userbot` не показывает ERROR
6. ✅ Redis отвечает на PING

### Полная переустановка

```bash
# Если ничего не помогает
docker-compose down -v            # остановить и удалить volumes
rm voice_transcriber.session       # удалить сессию
docker system prune -a             # очистить Docker
docker-compose up -d --build       # пересоздать с нуля
```

### Обратиться за помощью

Соберите информацию:
```bash
{
  echo "=== System Info ===" 
  docker --version
  docker-compose version
  uname -a
  
  echo "=== Container Logs ===" 
  docker-compose logs --tail=100
  
  echo "=== Environment ===" 
  cat .env | grep -v HASH  # скрыть sensitive data
  
} > diagnostic.txt

# Отправить diagnostic.txt
```

---

## 🎉 Результат

После успешной установки вы получите:

✅ Полностью рабочий голосовой транскрибер  
✅ Автоматическое переподключение при разрыве сети  
✅ Graceful shutdown при остановке  
✅ Health checks и мониторинг  
✅ Полная документация  
✅ Готовность к production на Proxmox LXC  

---

## 📞 Дополнительно

### Обновление

```bash
# Чтобы обновить на новую версию
git pull origin main
docker-compose down
docker-compose up -d --build
```

### Миграция со старой версии

```bash
# Если у вас была старая версия
docker-compose down
cp userbot.py userbot_old_backup.py  # сохранить старую

# Теперь новая версия уже установлена
docker-compose up -d
```

### Резервная копия

```bash
# Сохранить сессию и данные Redis
docker-compose exec redis redis-cli BGSAVE
cp voice_transcriber.session voice_transcriber.session.backup

# Архивировать
tar czf backup.tar.gz voice_transcriber.session redis_data/
```

---

**Версия:** 2.0  
**Дата:** 2026-02-18  
**Статус:** ✅ Готово к установке на Proxmox LXC Docker

