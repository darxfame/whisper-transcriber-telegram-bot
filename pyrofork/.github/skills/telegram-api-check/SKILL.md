# Telegram Bot API Integration Check Agent

## Описание

Агент для проверки корректности интеграций с Telegram Bot API, особенно установки команд бота через `set_my_commands()`.

## Когда применять

- При работе с `set_my_commands()` и установкой команд бота
- При добавлении новых команд в бота
- При изменении обработчиков команд
- Перед коммитом изменений, связанных с командами бота
- При работе с любыми Telegram Bot API методами, требующими верификации

## Обязанности

1. **Проверка полноты списка команд**:
   - Найти все `CommandHandler` регистрации в `bot.py`
   - Найти все обработчики команд в `src/handlers/commands.py`
   - Убедиться что все команды включены в `BotCommand` список
   - Проверить что нет лишних команд в `BotCommand`

2. **Проверка верификации установки**:
   - Убедиться что после `set_my_commands()` есть вызов `get_my_commands()`
   - Убедиться что есть сравнение установленных и ожидаемых команд
   - Убедиться что есть логирование результатов проверки

3. **Проверка очистки старых команд**:
   - Убедиться что перед установкой новых команд выполняется очистка через `set_my_commands([])`

4. **Проверка обработки ошибок**:
   - Убедиться что все API вызовы обернуты в try/except
   - Убедиться что ошибки логируются с контекстом

## Чеклист проверки

### Для установки команд (`set_my_commands`):

- [ ] **Синхронизация команд (КРИТИЧНО!)**:
  - [ ] Все команды из `CommandHandler` включены в `BotCommand`
  - [ ] Нет команд в `CommandHandler`, которых нет в `BotCommand`
  - [ ] Нет команд в `BotCommand`, которых нет в `CommandHandler`
  - [ ] Проверка синхронизации выполнена в коде (автоматическая проверка)

- [ ] **Полнота списка команд**:
  - [ ] Все команды из `CommandHandler` включены в `BotCommand`
  - [ ] Нет пропущенных команд
  - [ ] Нет лишних команд, которых нет в `CommandHandler`

- [ ] **Очистка старых команд**:
  - [ ] Перед установкой новых команд выполняется `set_my_commands([])`
  - [ ] Ошибки очистки обрабатываются (не критично, но желательно)

- [ ] **Установка команд**:
  - [ ] Выполняется `set_my_commands(all_commands)`
  - [ ] Логируется успешность установки
  - [ ] Логируется список установленных команд

- [ ] **Верификация установки (КРИТИЧНО!)**:
  - [ ] После `set_my_commands()` есть вызов `get_my_commands()`
  - [ ] Выполняется сравнение установленных и ожидаемых команд
  - [ ] Логируются отсутствующие или лишние команды
  - [ ] Ошибки проверки обрабатываются

- [ ] **Логирование**:
  - [ ] Логируется количество установленных команд
  - [ ] Логируется список команд (через `logger.info` или `logger.debug`)
  - [ ] Логируются предупреждения о несоответствиях

### Для других Telegram API операций:

- [ ] **Проверка результата**:
  - [ ] Результат API вызова проверяется на успешность
  - [ ] Пустые или неожиданные результаты логируются

- [ ] **Обработка ошибок**:
  - [ ] Все API вызовы обернуты в try/except
  - [ ] Специфичные ошибки Telegram обрабатываются отдельно (`RetryAfter`, `Forbidden`, `BadRequest`)
  - [ ] Общие ошибки логируются с `exc_info=True`

- [ ] **Логирование**:
  - [ ] Важные операции логируются с уровнем INFO
  - [ ] Детали операций логируются с уровнем DEBUG
  - [ ] Ошибки логируются с уровнем WARNING или ERROR

## Примеры правильной реализации

### Пример 1: Установка команд с полной проверкой

```python
async def setup_bot_commands(app):
    """Установка команд бота с полной проверкой"""
    try:
        # 1. Сбор всех команд
        commands = [
            BotCommand("rules", "Правила"),
            BotCommand("version", "Версия"),
            # ... все остальные команды
        ]
        
        # 2. Очистка старых команд
        try:
            await app.bot.set_my_commands([])
            logger.debug("Очищены старые команды")
        except Exception as e:
            logger.debug(f"Не удалось очистить команды (не критично): {e}")
        
        # 3. Установка новых команд
        await app.bot.set_my_commands(commands)
        logger.info(f"✅ Команды бота успешно обновлены")
        logger.info(f"📋 Установлено команд: {len(commands)}")
        logger.info(f"📋 Команды: {', '.join([f'/{cmd.command}' for cmd in commands])}")
        
        # 4. Верификация установки (КРИТИЧНО!)
        installed_commands = await app.bot.get_my_commands()
        installed_names = [cmd.command for cmd in installed_commands]
        expected_names = [cmd.command for cmd in commands]
        
        if set(installed_names) != set(expected_names):
            missing = set(expected_names) - set(installed_names)
            extra = set(installed_names) - set(expected_names)
            if missing:
                logger.warning(f"⚠️ Отсутствуют команды: {missing}")
            if extra:
                logger.warning(f"⚠️ Лишние команды: {extra}")
        else:
            logger.info(f"✅ Все команды установлены корректно")
            
    except Exception as e:
        logger.error(f"Ошибка установки команд: {e}", exc_info=True)
```

### Пример 2: Проверка полноты списка команд

```python
# Перед коммитом проверить:
# 1. Найти все CommandHandler в bot.py
command_handlers = [
    "rules", "complete", "version", "sync", 
    "cache_stats", "stats", "backup_db", "cleanup", "settings"
]

# 2. Проверить что все есть в BotCommand
bot_commands = [cmd.command for cmd in all_commands]
missing = set(command_handlers) - set(bot_commands)
if missing:
    raise ValueError(f"Отсутствуют команды в BotCommand: {missing}")
```

## Типичные ошибки

### ❌ Ошибка 1: Неполный список команд
```python
# Забыли добавить /settings
commands = [
    BotCommand("rules", "..."),
    BotCommand("version", "..."),
    # Нет /settings!
]
```
**Решение**: Проверить все `CommandHandler` в `bot.py` и включить все команды.

### ❌ Ошибка 2: Нет верификации
```python
await bot.set_my_commands(commands)
# Команды могут не установиться, но мы не узнаем
```
**Решение**: Добавить `get_my_commands()` и сравнение после установки.

### ❌ Ошибка 3: Нет очистки старых команд
```python
# Старые команды могут остаться
await bot.set_my_commands(new_commands)
```
**Решение**: Сначала очистить через `set_my_commands([])`.

### ❌ Ошибка 4: Нет логирования списка команд
```python
await bot.set_my_commands(commands)
logger.info("Команды установлены")
# Не видно какие команды установлены
```
**Решение**: Логировать список команд через `logger.info(f"Команды: {[cmd.command for cmd in commands]}")`.

## Автоматическая проверка перед коммитом

Перед коммитом кода с установкой команд:

1. **Найти все CommandHandler**:
   ```bash
   grep -r "CommandHandler" bot.py
   ```

2. **Сравнить с BotCommand**:
   - Убедиться что все команды из `CommandHandler` есть в `BotCommand`
   - Убедиться что нет лишних команд в `BotCommand`

3. **Проверить верификацию**:
   - Убедиться что есть вызов `get_my_commands()` после `set_my_commands()`
   - Убедиться что есть сравнение установленных и ожидаемых команд

4. **Проверить логирование**:
   - Убедиться что логируется список установленных команд
   - Убедиться что логируются предупреждения о несоответствиях

## Интеграция с другими агентами

Этот агент должен применяться вместе с:
- **code-review**: Для общей проверки кода перед коммитом
- **telegram-handler-agent**: Для проверки обработчиков команд
- **expert-agent-coding**: Для соблюдения стандартов кодирования

## Важно помнить

- **Верификация обязательна** - всегда проверяйте результат `set_my_commands()`
- **Полнота списка критична** - все команды из `CommandHandler` должны быть в `BotCommand`
- **Логирование помогает** - логируйте список команд для отладки
- **Telegram кэширует команды** - это нормально, пользователю нужно обновить клиент
