# 🎙️ Голосовой Транскрибер для Telegram

Userbot на базе Pyrofork с распознаванием голосовых сообщений (Whisper), автоматической пунктуацией и разбиением на абзацы.

## 📁 Структура проекта

```
pyrofork/
├── src/
│   └── userbot.py                  # Основное приложение
├── scripts/
│   ├── deploy.sh                   # 🚀 Деплой + автоочистка образов
│   ├── setup_docker_prune_cron.sh  # 🕐 Еженедельная автоочистка (cron)
│   ├── auth_docker.py              # 🔐 Авторизация внутри Docker
│   ├── start.sh                    # Запуск для Linux/Docker
│   └── health_check.sh             # Проверка состояния
├── docs/                           # 📚 Документация
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
└── .env                            # Переменные окружения (создать вручную)
```

---

## 🔐 Шаг 1 — Авторизация (первый запуск)

Создай `.env` файл:

```bash
API_ID=123456
API_HASH=твой_hash
FRIEND_USER_ID=0
HF_TOKEN=
```

Запусти авторизацию **внутри контейнера**:

```bash
docker-compose run --rm userbot python scripts/auth_docker.py
```

- Telegram пришлёт **5-значный код** в приложение (чат «Telegram»)
- Если есть 2FA — введи пароль
- Session сохранится в `./session/voice_transcriber.session`

> ⚠️ После авторизации файл `session/voice_transcriber.session` **не трогай** при редеплоях — он монтируется как volume.

---

## 🚀 Шаг 2 — Деплой

```bash
# Исправить окончания строк (нужно один раз на Linux-сервере после git clone/pull с Windows)
sed -i 's/\r//' scripts/deploy.sh scripts/start.sh && chmod +x scripts/*.sh

# Деплой
./scripts/deploy.sh
```

Скрипт сам:
1. Включает BuildKit
2. Собирает и поднимает контейнеры
3. Удаляет мусорные образы после сборки
4. Показывает занятое место на диске

**Проверка логов после деплоя:**
```bash
docker-compose logs -f userbot
```

Ожидай строку:
```
✅ Авторизован: <твоё имя> (ID: ...)
✅ Бот готов! Жду голосовые и команды...
```

---

## 🔧 Управление ботом

```bash
docker-compose up -d          # запустить
docker-compose down           # остановить
docker-compose restart        # перезапустить
docker-compose logs -f        # логи
docker-compose ps             # статус контейнеров
```

---

## 📋 Команды бота

> Команды работают только в **Избранном** (Saved Messages)

```
/help                         — список команд
/status                       — статус бота
/voicebot_on                  — включить всё
/voicebot_off                 — выключить всё
/my_on | /my_off              — свои голосовые вкл/выкл
/friend_on | /friend_off      — чужие голосовые вкл/выкл
/model                        — текущая модель Whisper
/model <имя>                  — сменить модель
/addtovoicebot                — добавить пользователя (ответом или ID)
/delfromvoicebot              — удалить пользователя
/listvoicebot                 — список отслеживаемых
```

---

## ⚙️ Переменные окружения

```bash
API_ID=xxx                    # https://my.telegram.org/apps
API_HASH=xxx
WHISPER_MODEL=medium          # tiny/base/small/medium/large-v2/large-v3
FRIEND_USER_ID=0              # ID пользователя (опционально)
HF_TOKEN=hf_xxx               # HuggingFace token (опционально, ускоряет загрузку)
```

---

## ⚡ Кэш зависимостей (быстрый редеплой)

Используется **BuildKit cache mounts** — pip и apt пакеты хранятся на сервере между сборками:

| Сценарий | Время |
|----------|-------|
| Первая сборка | ~5–10 мин |
| Редеплой без изменений в `requirements.txt` | ~10–20 сек |
| Редеплой с новым пакетом | ~30 сек (качает только новый) |

Кэш хранится в `/var/lib/docker/buildkit/` и управляется Docker автоматически.

**Важно**: BuildKit переменные `DOCKER_BUILDKIT=1` **должны быть экспортированы перед** вызовом `docker-compose`. Скрипт `deploy.sh` это делает автоматически через `.env.build` файл.

---

## 🧹 Автоочистка Docker-мусора

**При каждом деплое** `deploy.sh` автоматически удаляет dangling-образы.

**Еженедельная очистка по cron** (настроить один раз на сервере):
```bash
bash scripts/setup_docker_prune_cron.sh
```
Каждое воскресенье в 3:00 удаляет все образы старше 7 дней.

**Ручная полная очистка** (если нужно освободить место прямо сейчас):
```bash
docker system prune -f
```

> ⚠️ `docker system prune -a` удалит BuildKit-кэш pip/apt — следующая сборка будет долгой.

---

## 🐛 Частые проблемы

### `$'\r': command not found` при запуске `.sh` скриптов
Файл сохранён с Windows-окончаниями (CRLF). Исправить:
```bash
sed -i 's/\r//' scripts/deploy.sh && bash scripts/deploy.sh
```

Чтобы не повторялось после каждого `git pull`:
```bash
apt-get install -y dos2unix
cat > .git/hooks/post-merge << 'EOF'
#!/bin/bash
find . -name "*.sh" -not -path "./.git/*" | xargs dos2unix -q 2>/dev/null
chmod +x scripts/*.sh
EOF
chmod +x .git/hooks/post-merge
```

### `the --mount option requires BuildKit`
Старый `docker-compose` v1 не включает BuildKit автоматически. `deploy.sh` уже содержит фикс (`DOCKER_BUILDKIT=1`). Если запускаешь вручную:
```bash
DOCKER_BUILDKIT=1 COMPOSE_DOCKER_CLI_BUILD=1 docker-compose up -d --build
```

### `debconf: unable to initialize frontend: Dialog`
Не ошибка — предупреждение. Уже исправлено в `Dockerfile` через `DEBIAN_FRONTEND=noninteractive`.

### Бот запускается, шлёт тест, но не реагирует на команды и голосовые
Причина — ручной вызов `raw.functions.updates.GetState()` десинхронизирует внутренние счётчики Pyrofork. Уже исправлено в `src/userbot.py`.

### Redis недоступен при старте
`docker-compose.yml` содержит `healthcheck` на Redis и `condition: service_healthy` — бот стартует только после готовности Redis.

---

## 📊 Требования к серверу

| Параметр | Минимум | Рекомендуется |
|----------|---------|---------------|
| RAM | 4 GB | 16 GB |
| CPU | 2 ядра | 8 ядер |
| Диск | 10 GB | 40 GB |
| Docker | 20.10+ | 24.0+ |

---

## ✨ v2.2 — Изменения (февраль 2026)

✅ Исправлен root-cause: `GetState()`/`UpdateStatus()` десинхронизировали update stream  
✅ Redis healthcheck — бот не стартует раньше Redis  
✅ `HF_HOME=/app/models` — модели Whisper кэшируются в персистентный volume  
✅ BuildKit cache mounts — apt и pip кэшируются между сборками  
✅ `DEBIAN_FRONTEND=noninteractive` — убраны предупреждения debconf  
✅ `asyncio.create_task` защищён от GC через `_background_tasks`  
✅ `split_text()` вызывается один раз вместо N+1  
✅ `deploy.sh` — деплой + автоочистка образов в одной команде  
✅ `.gitattributes` — LF для всех `.sh`/`.py`/`.yml` файлов  

---

**Версия:** 2.2 | **Статус:** ✅ Production Ready